name: 幣安監控 - 主系統

on:
  schedule:
    - cron: '*/2 * * * *'  # 每2分鐘一次
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    
    steps:
      - name: 1. 隨機延遲啟動
        run: |
          echo "🕐 隨機延遲啟動..."
          sleep $((RANDOM % 15 + 10))
          echo "✅ 延遲結束"

      - name: 2. 檢出代碼
        uses: actions/checkout@v3

      - name: 3. 設置 Python 環境
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          # 移除 cache 設定，避免錯誤

      - name: 4. 安裝必要套件
        run: |
          echo "📦 安裝必要套件..."
          pip install python-binance==1.0.19 requests==2.31.0
          echo "✅ 套件安裝完成"

      - name: 5. 執行智能監控
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          MONITOR_MODE: "main"
        run: |
          echo "🚀 啟動智能監控系統..."
          python binance_smart_monitor.py
