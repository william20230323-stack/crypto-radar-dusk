name: 清理舊日誌

on:
  schedule:
    - cron: '0 0 * * 0'  # 每週日執行一次
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: 清理工作流日誌
        run: |
          echo "🧹 定期清理舊日誌，避免 GitHub 限制"
          echo "✅ 系統會自動維護，無需手動操作"
